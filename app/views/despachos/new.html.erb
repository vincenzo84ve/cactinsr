<h1>New despacho</h1>

<% form_for(@despacho) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :fecha %><br />
    <%= calendar_date_select_tag "despacho[fecha]", Date.today("GREGORIAN").to_s+" 00:00", :time => true %>
  </p>
  <p>
    <%= f.label :persona_id %><br />
    <%= collection_select(:despacho, :persona_id, @personas, :id, :nombre_apellido) %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', despachos_path %>